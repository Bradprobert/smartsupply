import { Dimensions } from 'react-native';

export const constants = {
    SCREEN_WIDTH: Dimensions.get('window').width,
    SCREEN_HEIGHT: Dimensions.get('window').height,
    MODAL_MARGIN_PCT: 0.10,
    BORDER_RADIUS: 10
};
